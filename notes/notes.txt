mogrify -fill transparent -fuzz 10% +opaque black *.png

#caches
php artisan route:cache
php artisan config:cache

php artisan optimize

#clear caches
php artisan cache:clear
php artisan view:clear
php artisan route:clear
php artisan clear-compiled
php artisan config:cache


• Service Providers are loaded at the very start and supposed to add to the framework capabilities needed in your app
• Models usually, interfaces with a data storage
• Controllers are supposed to process requests from users and, using the provided capabilities, return to the users corresponding answers
• Views usually store the HTML of your page and are the presentation layer of the MVC architecture
